import React from 'react';
import {Typography} from "@material-ui/core";

function File(props) {
    return (
        <Typography variant={'h6'}>
            Files
        </Typography>
    );
}

export default File;